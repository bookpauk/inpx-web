// initial state
const state = {
    config: {},
};

// getters
const getters = {};

// actions
const actions = {};

// mutations
const mutations = {
    setConfig(state, value) {
        state.config = value;
    },
};

export default {
    namespaced: true,
    state,
    getters,
    actions,
    mutations
};
